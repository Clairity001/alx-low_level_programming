#include "main.h"
/**
 * _isupper - evaluates if letter is uppercase
 * Description: prints the alphabet with _putchar
 * @c: character
 * Return: void
 */

int _isupper(int c)
{
	if (c >= 'A' && <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
